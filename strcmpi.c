#include<stdio.h>
int main()
{
	int res=strcmpi("first str",second str);
	if(res=0)
	printf("strings are equal");
	else
	printf("strings are unequal");
	printf("\n value of result: %d",res);
	return 0;
}
